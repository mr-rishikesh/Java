{
  "name": "prep-dashboard-root",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "mongoose": "^8.3.1"
  },
  "scripts": {
    "build": "cd frontend && npm install && npm run build"
  }
}
